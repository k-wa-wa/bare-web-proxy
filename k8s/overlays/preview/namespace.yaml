# 名前は ApplicationSet の kustomize.patches で bare-web-proxy-pr-<N> に置換される。
# Namespace自体をApplicationの管理下に置くことで、PR close時に resources-finalizer が
# Namespaceごと削除できるようにする。
apiVersion: v1
kind: Namespace
metadata:
  name: bare-web-proxy-preview
