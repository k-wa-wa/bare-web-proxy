{
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && esbuild toolbar.ts --bundle --minify --target=es2020 --loader:.css=text --outfile=dist/toolbar.js && cp reader.css index.html dist/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.25.5",
    "typescript": "5.8.3"
  }
}