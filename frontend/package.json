{
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && esbuild toolbar.ts --minify --target=es2020 --outfile=dist/toolbar.js && cp reader.css index.html dist/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.25.5",
    "typescript": "5.8.3"
  }
}